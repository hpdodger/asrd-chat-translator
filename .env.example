# Path to game data folder on the HOST machine (mounted into the container as /gamedata)
HOST_GAME_DATA_PATH=C:/path/to/game/reactivedrop/save/vscripts

# Languages to load in LibreTranslate — should match loadOnly in app-config.json
LT_LOAD_ONLY=en,ru,zh,de
