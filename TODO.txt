1. причесать код squirrel - вынести все в отдельный модуль, чтобы не засорять глобальный мапсппавн
2. причесать код js (D:\private\asrd\asrd-chat-translator\node\translator.js):
    V 2.0 переписать на ts
    V 2.1 разбить на модули 
    V 2.2 сделать абстракцию над переводчиком (на случай подключения другого). предлагаемый путь - базовый абстрактный класс BaseTranslator (принимает некий json конфиг), от него наследник LibreTranslator      
    V 2.3 комментарии и стилистика кода - комментарии должны быть на английском, объяснять почему сделано так, а не иначе и только в местах, где логика может быть неочевидна, кавычки должны быть двойные